# This directory holds optional pre-trained GAN model weights.
# If no models are present, the age progression module will
# automatically use its OpenCV/PIL image-processing fallback.
#
# Supported model files:
#   - hrfae_best.pth       (HRFAE GAN checkpoint)
#   - deploy.prototxt      (DNN face detector – auto-downloaded)
#   - res10_300x300_ssd_iter_140000.caffemodel (auto-downloaded)
